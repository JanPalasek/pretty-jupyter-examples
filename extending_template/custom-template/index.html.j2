{%- extends "pj/index.html.j2" -%}

{% block page_header %}
<div>
    <hr />
    <h1 class="title">{{ nb.metadata.get('title', '') or resources['metadata']['name'] }}</h1>
    <h4> Author: {{ nb.metadata.get("author", "") }}</h4>
    <h4> Generated: {{ resources['date'].strftime("%Y-%m-%d %H:%M:%S") }} </h4>
    <hr />
</div>
{% endblock page_header %}

{% block notebook_css %}
    {{ super() }}

    <style>
        .nav-pills>li.active>a,
        .nav-pills>li.active>a:hover,
        .nav-pills>li.active>a:focus {
        background-color: black;
        }
        a,
        a:hover {
        color: black;
        }
        .list-group-item.active,
        .list-group-item.active:hover,
        .list-group-item.active:focus {
        background-color: black;
        border-color: black;
        }
        .nav-tabs>li.active>a, .nav-tabs>li.active>a:focus {
        border: none;
        -webkit-box-shadow: inset 0 -2px 0 black;
        box-shadow: inset 0 -2px 0 black;
        color: black;
        }
        .nav-tabs>li.active>a:hover, .nav-tabs>li.active>a:focus:hover {
        border: none;
        color: black;
        }
        .nav-tabs>li>a:hover, .nav-tabs>li>a:focus:hover {
        background-color: transparent;
        -webkit-box-shadow: inset 0 -2px 0 black;
        box-shadow: inset 0 -2px 0 black;
        color: black;
        }
    </style>

{% endblock notebook_css %}
